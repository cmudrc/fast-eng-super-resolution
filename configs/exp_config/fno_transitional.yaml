# must have configs
n_clusters: 3
save_mode: 'save_png'

# encoder configs
n_components: 20
domain_size: 0.1963495

# classifier configs

# model configs
in_channels: 8
out_channels: 8
width: 20

# data configs
root: 'data/jhtdb_transition_bl'
tstart: 1
tend: 100
fields: 'u'
dataset: 'transition_bl'
partition: True
sub_size: 12

# pred configs
idxs: [30]
timesteps: 4